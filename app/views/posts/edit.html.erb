<h1 class="card-title fw-bold fs-4">Editing Post</h1>

<%= render 'form' %>

<p class="fw-light mt-3">
  <%= link_to 'Show', forum_post_path(@post) %> |
  <%= link_to 'Back', forum_path(@post.forum) %>
</p>
